# Agent Contributor Guide

{{ project_name }}

{{ project_description }}

This repository contains the `{{ package_name }}` python package and an associated Snakemake workflow.

This project was created with the [able-workflow-copier](https://github.com/NEU-ABLE-LAB/able-workflow)
suite of Copier templates for creating ETL processes and workflows with Snakemake and Python.
For more information on the templates applied see the copier answers in the `./copier-answers/`
directory (DO NOT EDIT THESE FILES) and the documentation for each applied template in the
`template/docs/docs/contributing/templates/` directory.

## Envornment

The setup script `AGENTS-setup.sh` should have already been run, setting up the environment as follows:

- The `.bashrc` file has been modified to activate the conda environment `{{ package_name }}-py312-dev-runner` 
  that includes all the development dependencies.
- `tox` is available for environment isolation fur testing

## The `{{ package_name }}` python package

This package contains the `runner` extras that are specified in `pyproject.toml`.
Sub-modules that require extras reside in a `runner/` subdirectory.

## Snakemake workflows

Many of the snakemake rules require specialized conda environments as specified by the `conda:` directive
that are available (after environment initialization and localization) in the `workflow/envs/localized` directory.

## Testing

Tox is used to run all the tests. The environment initialization already installed all the packages.
The rule integration tests depend on snakemake which requires an internet connection to create
conda environments, so skip those. To speed things up, run tox in parallel and skip the rule integration environments:

```bash
tox run-parallel --parallel auto --parallel-no-spinner --skip-pkg-install --skip-env='py.*-workflow-rules'
```

Parallel execution is safe because each tox environment writes its own files in `.cover/` and `.junit/`.
Directory creation uses `mkdir -p`, so races are avoided. If any environment needs exclusive access to a resource,
add a check in its `commands_pre` section and raise an error.
