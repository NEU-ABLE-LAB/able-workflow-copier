# Workflow configuration

TODO-copier-package explain default profile (`workflow/profiles/default/config.yaml`) and SLURM profile ((`workflow/profiles/slurm/config.yaml`)). Consider adding `export SNAKEMAKE_PROFILE=slurm` to someone automatically detetect if the machine has SLURm.


TODO-copier-package explain configuration files (`config/`) and schemas (`workflow/schema)
