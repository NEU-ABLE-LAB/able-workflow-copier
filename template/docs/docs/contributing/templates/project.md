# Project Template

This template created the main project structure.

## Updating

Update the project with changes to [`able-workflow-copier`]({{ able_workflow_copier_docs }}) by running the following command in the `{{ package_name }}-py312-dev` conda environment.

```bash
copier update --trust --answers-file ".copier-answers/project.yml"
```
