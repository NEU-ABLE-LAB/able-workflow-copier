"""
The tests in this directory are designed to validate thee functionaltity
of conda rules that only require the core dependencies of the
{{ package_name }} package.

Primarily, these tests will interact with the `extract.py`
and `schema.py` files within ETL processes within the package.
That is, these tests will not extract external data,
transform data, or load data to disk.
"""
